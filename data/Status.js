const STATUS_ORDER = {
    UNPAID: "UNPAID",
    PAID: "PAID",
    IN_PROGRESS_DELIVERY: "IN_PROGRESS_DELIVERY",
    DELIVERED: "DELIVERED",
    CANCELED: "CANCELED"
};

const STATUS_ORDER_ENUM = Object.values(STATUS_ORDER);

module.exports = { 
    STATUS_ORDER, STATUS_ORDER_ENUM
};